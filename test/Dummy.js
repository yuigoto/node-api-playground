/**
 * API PLAYGROUND : Dummy
 * ----------------------------------------------------------------------
 * Apenas um boneco para arquivos de teste.
 * 
 * @author    SomeNameHere <name@domain>
 * @since     0.0.1
 */

// Define environment como "test" (sobrescreve `.env`)
process.env.NODE_ENV = "test";

// Bibliotecas
import { expect, should } from "chai";
import mongoose from "mongoose";

// Imports locais

// Inicializando Should
should();

// Função com testes
const Test = () => {
};

// Exportando testes
export default Test;
